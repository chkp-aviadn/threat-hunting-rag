# Core dependencies
pandas>=1.5.0
numpy>=1.24.0           # Vector operations and embeddings
faker>=19.0.0
tldextract>=3.4.0
sentence-transformers>=2.2.0
chromadb==1.3.4
# SQLite compatibility fix for ChromaDB (single entry)
pysqlite3-binary>=0.5.2
# OpenTelemetry: pin to version range compatible with chromadb 1.3.4 and drop instrumentation extras for now
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
# (Removed instrumentation packages temporarily to avoid BoundedAttributes import error)
python-dotenv>=1.0.0
requests>=2.31.0        # HTTP requests for API testing and domain validation
email-validator>=2.0.0  # Email validation utilities
tqdm>=4.66.0            # Progress bars for data processing

# API Framework (Primary Interface)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
pydantic-settings>=2.0.0

# API Features & Middleware
slowapi>=0.1.9          # Rate limiting
python-multipart>=0.0.6 # Form data support
httpx>=0.27.0           # Compatible with chromadb (>=0.27.0) and pytest-httpx (0.27.*)

# Testing & Development
pytest==7.4.4
pytest-asyncio==0.21.1  # Async test support
pytest-httpx==0.30.0    # Compatible with httpx 0.27.x

# Security dependencies
cryptography>=41.0.0    # Data encryption and secure hashing

# Code quality tools
black>=23.0.0           # Code formatting
flake8>=6.0.0           # Linting
mypy>=1.5.0             # Type checking
pytest-cov==4.1.0      # Coverage reporting
bandit>=1.7.0          # Security vulnerability scanner

# Performance & Caching
psutil>=5.9.0           # System monitoring
redis>=5.0.0            # Redis client for optional session/cache backend
